---
- hosts: local
  become: yes

  tasks:
    - include_role:
        name: common
        
    - include_role:
        name: chrome
        
    - include_role:
        name: programming
      vars:
        anaconda_version: "2020.07"
        
    - include_role:
        name: writing
      vars:
        zotero_version: "5.0.89"

    # this assumes that an account 'jacquem' has been created
    - include_role:
        name: user
      vars:
        user_login: jacquem
        user_name: "Vincent Jacquemet"
        user_email: "vincent.jacquemet@umontreal.ca"
    
