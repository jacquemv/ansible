---
- hosts: local
  become: yes

  tasks:
    - include_role:
        name: workstation
      vars:
        anaconda_version: 2019.07
        zotero_version: "5.0.74"
    
    # this assumes that an account 'jacquem' has been created
    - include_role:
        name: user
      vars:
        user_login: jacquem
        user_name: "Vincent Jacquemet"
        user_email: "vincent.jacquemet@umontreal.ca"
  
